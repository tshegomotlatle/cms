import { DocumentsRepository } from './documents-repository';

describe('DocumentsRepository', () => {
  it('should be defined', () => {
    expect(new DocumentsRepository()).toBeDefined();
  });
});
