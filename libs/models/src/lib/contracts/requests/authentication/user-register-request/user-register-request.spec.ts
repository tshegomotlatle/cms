import { UserRegisterRequest } from './user-register-request';

describe('UserRegisterRequest', () => {
  it('should be defined', () => {
    expect(new UserRegisterRequest()).toBeDefined();
  });
});
